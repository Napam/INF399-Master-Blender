The Python files are meant to be interpreted with Blender's 
Python interpreter. In PowerShell you can make an environment
variable with the path of Blender's python and then use that as 
your Python interpreter. 